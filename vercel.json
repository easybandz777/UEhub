{
  "version": 2,
  "name": "ue-hub-frontend",
  "buildCommand": "pwd && ls -la && test -d frontend && echo 'Frontend dir exists' || echo 'Frontend dir missing' && test -f frontend/package.json && echo 'Package.json exists' || echo 'Package.json missing' && cd frontend && pwd && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "echo 'Starting build process'",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://uehub.fly.dev/v1/:path*"
    }
  ]
}
