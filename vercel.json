{
  "version": 2,
  "name": "ue-hub-frontend",
  "buildCommand": "cp package-vercel.json package.json && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cp package-vercel.json package.json",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://uehub.fly.dev/v1/:path*"
    }
  ]
}
